import React, { useContext } from "react";
import { useNavigate } from "react-router-dom";

function UserSettlements() {
  let navigate = useNavigate();

  return <div id="UserSettlements"></div>;
}

export default UserSettlements;
